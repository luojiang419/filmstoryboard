# 117 - 版本1.0.0.322安装包编译完成

生成时间：2026-08-19 21:16:30

## 已完成内容

- 已将版本从 `1.0.0+321 / 1.0.0.321 / v1.0.0.321` 同步递增到 `1.0.0+322 / 1.0.0.322 / v1.0.0.322`。
- 已执行 `D:\flutter\bin\flutter.bat pub get`。
- 已执行 `scripts/ensure_ffmpeg_windows.ps1`，确认发布包内 FFmpeg 资源可用。
- 已执行 `D:\flutter\bin\flutter.bat build windows --release`，成功生成 `build/windows/x64/runner/Release/filmstoryboard.exe`。
- 已执行 `iscc installer/filmstoryboard.iss`，成功生成 `dist/installer/filmstoryboard-Setup-1.0.0.322.exe`。
- 已执行 `scripts/verify_release_artifact.ps1 -Version 1.0.0.322`，校验通过并生成 SHA-256 校验文件。
- 已补充本轮增量备份与避坑记录文档。

## 当前模块

Windows 发布构建模块

## 当前 Git 状态

```text
 M installer/filmstoryboard.iss
 M lib/features/updater/domain/app_update_config.dart
 M pubspec.yaml
?? backup/067-版本322安装包编译增量备份.md
?? 进度快照/117-版本1.0.0.322安装包编译完成.md
?? 避坑指南/104-PowerShell发布产物枚举需复查真实文件.md
```

## 代码前后对比 / Diff

```diff
diff --git a/installer/filmstoryboard.iss b/installer/filmstoryboard.iss
index 326fd02..a23039a 100644
--- a/installer/filmstoryboard.iss
+++ b/installer/filmstoryboard.iss
@@ -1,5 +1,5 @@
 #define MyAppName "filmstoryboard"
-#define MyAppVersion "1.0.0.321"
+#define MyAppVersion "1.0.0.322"
 #define MyAppPublisher "Jiang"
 #define MyAppExeName "filmstoryboard.exe"
 
diff --git a/lib/features/updater/domain/app_update_config.dart b/lib/features/updater/domain/app_update_config.dart
index 3129dc4..6f13f9b 100644
--- a/lib/features/updater/domain/app_update_config.dart
+++ b/lib/features/updater/domain/app_update_config.dart
@@ -3,8 +3,8 @@ class AppUpdateConfig {
 
   static const appName = 'filmstoryboard';
   static const userAgent = 'filmstoryboard';
-  static const currentVersion = '1.0.0.321';
-  static const currentVersionTag = 'v1.0.0.321';
+  static const currentVersion = '1.0.0.322';
+  static const currentVersionTag = 'v1.0.0.322';
   static const windowTitle = '$appName $currentVersionTag';
   static const installerBaseName = 'filmstoryboard-Setup';
   static const defaultReleaseRepositoryUrl =
diff --git a/pubspec.yaml b/pubspec.yaml
index 33b13fe..6bfe010 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -16,7 +16,7 @@ publish_to: 'none' # Remove this line if you wish to publish to pub.dev
 # https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
 # In Windows, build-name is used as the major, minor, and patch parts
 # of the product and file versions while build-number is used as the build suffix.
-version: 1.0.0+321
+version: 1.0.0+322
 environment:
   sdk: ^3.10.7
```

## 验证结果

- `D:\flutter\bin\flutter.bat pub get`：通过。
- `D:\flutter\bin\flutter.bat build windows --release`：通过。
- `build/windows/x64/runner/Release/filmstoryboard.exe`：`FileVersion=1.0.0+322`，`ProductVersion=1.0.0+322`。
- `iscc installer/filmstoryboard.iss`：通过。
- `scripts/verify_release_artifact.ps1 -Version 1.0.0.322`：通过。
- 安装包：`dist/installer/filmstoryboard-Setup-1.0.0.322.exe`
- 校验和：`42cd2f0347f0932122dbd9868b5fe0cbe4bb94d0958b5be4ff55e284265bb239`
- 安装包大小：`422729141` 字节。
- 签名状态：`NotSigned`。

## 待办清单

- 如需把本轮版本变更持久化到远端仓库，执行提交并推送。
- 如需对外分发，补充发布说明或上传安装包到发布渠道。

## 下一步

可直接安装并验收 `dist/installer/filmstoryboard-Setup-1.0.0.322.exe`；若需要继续自动备份源码到 GitHub，下一步执行提交与推送。

## 避坑记录

- 已新增 `避坑指南/104-PowerShell发布产物枚举需复查真实文件.md`。
- 本轮 `verify_release_artifact.ps1` 已成功生成 `.sha256` 文件，但单条 `Get-ChildItem` 混合枚举命令一度误报缺失；发布验收时应以校验脚本输出和目录复查双重确认。
