# 故事板接收器启动必须容错 Provider 缺失

## 问题

部分简化 Provider 测试夹具没有注册 `projectDirectoriesProvider`。如果在 `initState` 无保护地读取 Provider，故事板页面会在测试或异常启动场景直接抛错。

## 解决

- 将 Provider 读取、接收器创建和 `start()` 全部放入 `try/catch`。
- 端口占用、目录不可用和启动异常只表示自动接收不可用，手工导入仍保持可用。
- 临时包删除放在接收回调的 `finally`，避免成功和失败路径产生垃圾文件。
