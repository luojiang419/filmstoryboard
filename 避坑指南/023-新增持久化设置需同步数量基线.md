# 023 - 新增持久化设置需同步数量基线

## 现象

新增设置字段后，功能定向测试和静态分析均通过，但全量测试中的 `settings` 表记录数量断言失败。

## 根因

`SettingsRepository.save` 会为每个持久化字段写入一条键值记录。新增设置键后，只验证字段读写还不够，固定数量基线也必须同步。

## 规避

- 新增设置键时搜索 `countRows('settings')` 和所有固定数字断言。
- 同时增加新字段的默认值、保存值和重新读取断言。
- 修改后必须执行全量测试，定向测试无法发现跨模块数量基线。
