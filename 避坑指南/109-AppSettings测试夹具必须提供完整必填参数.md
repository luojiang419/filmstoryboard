# 109 - AppSettings 测试夹具必须提供完整必填参数

## 问题

新服务测试直接使用 `const AppSettings()` 会编译失败，因为 `AppSettings` 有多项 required 构造参数。

## 解决

复用仓库既有 `_settings()` 测试夹具模式，显式提供导出目录、主题、裁切编号、视觉模型、图片模型和更新配置等必填项。

## 规避

- 新增依赖 `AppSettings` 的服务测试时不要假设存在空构造或默认工厂。
- 优先参考同目录现有测试的最小完整夹具。
