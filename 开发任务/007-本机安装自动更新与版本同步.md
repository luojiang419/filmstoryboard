# 本机安装自动更新与版本同步

状态：已完成
当前阶段：4/4
最后更新：2026-09-06 09:00

## 当前状态

划像对比修复已构建为 `1.1.0.372` 并自动部署到 `D:\Program Files\filmstoryboard`。应用 EXE 的产品和文件版本、Windows 卸载注册表版本、窗口标题均已验证为 `1.1.0.372`；115 个更新文件已逐项通过 SHA-256 校验。

## 下一步

当前任务已完成。后续新增功能或修复时，递增四段版本末位、构建后复用本流程自动更新本机安装目录。

## 当前 TODO

- [x] 检查已安装目录、安装记录与现有增量更新机制
- [x] 注入并校验 `1.1.0.372` 版本
- [x] 构建 Windows Release
- [x] 自动更新本机安装目录与版本记录
- [x] 运行验证、Git commit / push

## 最近验证状态

- 静态检查：`flutter analyze --no-pub` 通过，无问题
- 定向回归：`storyboard_image_comparison_test.dart` 2 项通过
- 构建：`flutter build windows --release --no-pub --build-name 1.1.0 --build-number 372` 通过（44.2 秒）
- 本机部署：115 个应用文件 SHA-256 校验通过，备份位于 `dist/local-update-backup/1.1.0.372-20260906-085406`
- 运行验证：已安装路径进程 Responding=True，窗口标题 `filmstoryboard v1.1.0.372`
- 已安装应用版本：`1.1.0.372`
- 卸载注册表版本：`1.1.0.372`
- 安装目录：`D:\Program Files\filmstoryboard`
- branch：`feat/paired-wardrobe-storyboard-depth`
- 版本同步前 Git commit：`b5c2691`

---

## 任务目标

将当前代码自动部署到已安装的 `D:\Program Files\filmstoryboard`，无需手动运行安装包，并保证应用 EXE 与 Windows 卸载记录的版本完全同步。

## 当前项目现状

- `scripts/update_local_installation.ps1` 已具备受控增量部署、备份、SHA-256 校验、失败回滚和注册表版本同步能力。
- 脚本只覆盖 EXE、DLL、native assets、Flutter 运行时载荷，保留用户数据、模型权重、网页资产及卸载器。
- 当前功能修复后的源码版本仍是 `1.1.0.371`，需递增四段版本末位后构建部署。

## 技术方案

- 使用 `scripts/prepare_release.py` 将 `pubspec.yaml`、应用更新配置与 Inno Setup 版本统一为 `1.1.0.372`。
- 通过 Flutter Windows Release 构建生成带该文件版本的应用载荷。
- 先以 `CheckOnly` 校验更新清单，再执行本机更新脚本；脚本会在替换前关闭已安装程序、备份旧文件、逐项校验哈希并写入注册表版本。
- 最后读取 EXE 与卸载注册表，验证均为 `1.1.0.372`。

## 文件 / 模块清单

- 修改：`pubspec.yaml`、`lib/features/updater/domain/app_update_config.dart`、`installer/filmstoryboard.iss`
- 新增：本任务文档
- 执行：`scripts/update_local_installation.ps1`

## 开发阶段

- [x] 阶段 1：现状与更新机制确认
- [x] 阶段 2：版本注入与构建
- [x] 阶段 3：本机更新与版本核验
- [x] 阶段 4：提交、推送与验收

## 验收标准

1. `D:\Program Files\filmstoryboard\filmstoryboard.exe` 的产品版本为 `1.1.0.372`。
2. 卸载注册表 DisplayVersion 为 `1.1.0.372`，且安装路径仍为目标目录。
3. 更新载荷逐项通过 SHA-256 校验，旧载荷存在可恢复备份。
4. 已安装应用可以从目标目录启动。

## 已知问题

无。

## 开发日志

- 2026-09-06：将版本统一更新为 `1.1.0.372`，完成 Windows Release 构建和本机增量部署；安装目录与注册表版本同步成功。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话先阅读项目规则与本文件，检查 Git branch、`git status` 和版本来源；从“下一步”继续，不重复已完成阶段。
