# 深度模型自动下载与安装修复

状态：已完成
当前阶段：4/4
最后更新：2026-09-05 19:38

## 当前状态

- 已核对安装版组件、应用日志、数据库错误记录和 Windows 事件日志。
- 旧 367 安装包包含 1,785,796,464 bytes 的两个模型权重，且旧应用缺少模型时只报组件不完整。新 368 安装包已排除权重并具备自动补齐能力。
- 安装目录 `D:\Program Files\filmstoryboard\data` 的 ACL 允许普通用户修改；按用户要求继续把模型放在安装目录，不迁移用户缓存。
- 安装版 worker 的 `--smoke-test` 返回 exit code 0；旧应用没有深度组件日志，新服务已补齐。Windows 内存预警与本次两个已复现根因无直接证据联系。
- 已从实际打开工程 `G:\DATA\影视复刻 (2)\database\project.sqlite` 查到两次深度失败：`FormatException: Missing extension byte`。主数据库无错误不代表工程无错误。
- 根因一：冻结 worker 忽略 UTF-8 环境变量，以 Windows 本地编码读写，中文路径乱码引发文件不存在，错误响应又被 Dart UTF-8 解码失败。ASCII JSON 转义与本地编码响应解码已修复。
- 根因二：预览 Isolate.run 闭包捕获提取队列的 Completer，导致 unsendable。独立静态异步方法已修复。
- 真实安装版 worker + 原失败中文路径原帧 + Dart 服务端到端测试已通过：1920×1080 的 16-bit 母版和 8-bit 预览均有效。
- 自动下载、SHA-256、Range 续传、进度专用 ValueListenable 和独立 UI 已实现；避免下载进度频繁重建整个工作区。
- 1.1.0.368 Windows Release 与 Inno Setup 安装包均编译成功并通过版本、载荷和哈希校验。修复代码提交 `7e4faa5` 已推送。
- 用户安装目录的 EXE 未被自动覆盖，现有模型完整保留；交付新安装包供覆盖安装。

## 下一步

当前任务已完成。交付 `dist/installer/filmstoryboard-Setup-1.1.0.368.exe`；用户覆盖安装到原路径后，重新提取原先失败的镜头即可。已有模型校验后复用，缺失模型自动补齐。

## 当前 TODO

- [x] 核对安装目录、日志与真实 worker 状态
- [x] 实现模型自动下载、校验、断点续传与日志
- [x] 显示 0–100% 下载进度
- [x] 排除安装包模型文件并清理陈旧构建载荷
- [x] 相关测试与静态检查
- [x] Windows Release 和安装包构建验证
- [x] Git commit 与 push

## 最近验证状态

- 安装版 worker：`--smoke-test` exit code 0。
- 模型下载源：Hugging Face 固定 revision 的两个 URL 均返回 HTTP 200，Content-Length 与本地文件一致。
- 工程数据库：两次 `Missing extension byte` 失败已定位；未修改用户历史记录。
- 安装版 Dart 服务真实测试：4 项全部通过（包含原失败原帧的完整推理与预览转换）。
- 静态分析：No issues found。
- 全量测试：844 项通过、3 项跳过、0 失败。随后新增的真实网络测试按环境变量单独执行。
- 下载器与 UI：7 项通过。覆盖首次下载、Range 续传、忽略 Range、损坏模型重下、503 重试、哈希失败保护、0–100% UI。
- 真实网络：两份官方固定 revision 模型各续传最后 1 MB，完整 SHA-256 均匹配，通过后才报告 100%；本机原模型只读，测试副本已自动清理。
- Windows Release：`flutter build windows --release --no-pub` 通过，产品版本 `1.1.0+368`，深度组件目录没有 `.safetensors`。
- 安装包：Inno Setup 编译成功；1,701,972,898 bytes，SHA-256 `3e695e3b7751d4bdf65fd7e5c7d213a30c1124dbe5db1399b416350d5a6039d8`，签名状态 NotSigned。
- 产物校验：`verify_release_artifact.ps1 -Version 1.1.0.368` 通过；编译日志模型权重条目为 0，运行时和模型配置保留。
- 最终静态分析：No issues found；Git diff --check 通过。
- Windows 日志：发现一次 `RADAR_PRE_LEAK_64`，未发现崩溃记录。
- 当前 Git branch：`feat/precise-depth-replication`。
- 实现 Git commit：`7e4faa5 fix: repair depth worker Unicode and download missing models`，已推送；最终文档提交见 Git 历史。

---

## 任务目标

修复安装后深度图不可用的问题。安装包不包含模型权重；用户首次提取深度图时自动补齐模型，在原安装目录中断点下载并显示明确的 0–100% 进度，完成后继续提取，无需手动部署。

## 当前项目现状

- `PersonDepthService.resolveComponent()` 要求 worker 和两份模型同时存在，缺失即抛错。
- Windows Release 和 Inno Setup 都会递归复制完整 `person-depth`，导致安装包约 3.33 GB。
- 深度提取界面只有不确定进度的圆形动画。
- worker stderr 仅保存在内存最后 8 行，没有落盘诊断日志。

## 技术方案

- 使用模型官方 Hugging Face 固定 revision URL，下载到现有 `{app}\data\person-depth\models`。
- 下载写入 `.part` 文件，按已下载长度发起 Range 请求；服务端不支持 Range 时安全重下。
- 两份模型按总字节数统一计算进度；下载完成后核对大小和 SHA-256，再原子改名。
- 模型已存在时校验通过后报告 100%，随后启动 worker；下载失败保留合法 `.part` 供下次续传。
- 深度服务用专用 ValueListenable 输出模型准备进度和文案，精确资产区域显示线性进度条、百分比及下载字节。
- 深度服务关键阶段、worker stderr 和失败信息写入 `data/logs/person_depth.jsonl`。
- CMake 仅清理 Release 中两份陈旧权重和复制清单，复制组件时排除 `.safetensors`；Inno Setup 再次排除模型作为防线。不删除安装目录中的既有模型。

## 文件 / 模块清单

- `lib/features/replicate/data/person_depth_service.dart`
- `lib/features/replicate/application/replicate_controller.dart`
- `lib/features/replicate/presentation/replicate_page.dart`
- `test/features/person_depth_service_test.dart`
- `test/features/replicate_controller_test.dart`
- `test/features/replicate_page_test.dart`
- `windows/CMakeLists.txt`
- `installer/filmstoryboard.iss`
- `assets/person-depth/README.md`
- 版本配置与本任务文档

## 开发阶段

- [x] 阶段 1：下载器、校验和日志
- [x] 阶段 2：状态与进度 UI
- [x] 阶段 3：打包排除与测试
- [x] 阶段 4：Release、安装包与最终验收

## 验收标准

- 缺少模型时点击提取会自动下载，不再提示运行复制脚本。
- 下载过程中持续显示整数百分比，成功时显示 100%，随后自动继续深度推理。
- 中断后可从 `.part` 继续；错误下载不会成为正式模型文件。
- 下载后的两份模型大小和 SHA-256 与锁定值一致。
- Release 目录和安装包均不含 `.safetensors`，但 worker 与模型配置文件完整。
- 深度相关日志可在 `data/logs/person_depth.jsonl` 查看。
- 相关测试、静态检查、Windows Release 和 Inno Setup 编译通过。

## 已知问题

本次两个已复现故障均已修复，无未解决的开发阻塞。以下为交付边界：

- 深度运行时本身约 4.2 GB，仍会包含在安装包；本任务按用户要求只移除模型权重。
- Hugging Face 可用性取决于用户网络，下载失败会保留断点并给出可重试错误。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话读取项目规则、本任务文档和 Git 状态，从“下一步”继续，不重复已经完成的核验。
