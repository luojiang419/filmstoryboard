## 本次更新

- 一键复刻“准备资产”页的人物与产品资产参考图格子右上角新增移除按钮；点击后会立即移除对应格子，并同步清理该格子已经绑定的镜头资产，避免残留为补充资产格子。
- 一键复刻在图片生成并保存后立即标记完成并展示给用户查阅，不再发起后台生成后审核或自动续轮纠错；旧版中“成图已保留但审核失败”的记录，只要本地成图仍存在，重启后会自动恢复为已完成。
- 一键复刻“准备资产”的人物与产品动态资产格新增“保留（沿用原视频帧）”选项；保留原帧服装或主体时无需绑定替换资产，即使之前已经匹配资产也不会把替换图或细节图提交给生成模型。“替换”和“从画面移除”行为保持不变。
- 一键复刻“准备资产”不再显示固定的模特、产品、产品细节和场景预置格；原帧分析识别到的每个人物与产品会直接创建同名动态资产格，格内点击或拖入即可绑定替换资产，格子下方可直接选择“保留”“替换”或“移除”。补充资产与 DWPose 骨架仍按需保留。
- 修复单人物镜头识别出多件产品时的槽位数量错误：自动匹配现在按实际识别产品数创建产品 A/B/C……绑定顺序，生成前缺失绑定会准确指向对应动态格。
- 一键复刻的 Nano Banana 参考图在 20 MB 以内直接提交原始字节与原 MIME 类型，不再为了上传压缩而损失人物、产品材质和局部结构细节；超过 20 MB 时才进入原有压缩路径。
- 准备资产页对原帧中每个人物和产品新增“替换 / 保留 / 移除”必选决策；未完成决策、替换项未绑定资产或原帧分析过期时会阻止生成，避免原人物或原产品意外跑入复刻分镜。
- 多视图人物或产品拼图改为先由视觉模型结合原帧槽位选择最匹配的主视图和少量细节视图，再从本地原图无损裁切；主视图定义整体身份或产品外形，细节图只补充局部材质。
- 复刻默认继承原帧真实画幅，原帧只负责构图、机位、姿态、遮挡、光线和调色；人物身份与产品外观严格按逐项主体决策和绑定资产执行。
- 修正“确认镜头 → 构建脚本”的镜头时长策略：参考帧数量不再机械累加秒数，普通镜头按 2–5 秒的动作与运镜复杂度估算；人工填写的小数时长保持不变，旧版模型自动生成的超长镜头可在重新构建时按新规则更新。
- “设计分镜图”左侧参考图区支持从资源管理器直接拖入 PNG、JPG、JPEG、WEBP 或 BMP 图片；拖入时会高亮并提示松开添加，放下后沿用原有格式校验、去重和缩略图预览。
- “设计分镜图”的图片修改弹窗支持从资源管理器直接拖入 PNG、JPG、WEBP 或 BMP 参考图；拖入悬停时显示轻微呼吸微光和明确的松开提示，放下后自动去重添加，非图片文件不会误接收。
- 一键复刻“准备资产”针对 Google Nano Banana Pro / Gemini 3 改用受控多参考图复刻协议：图片1固定为唯一构图与摄影母版，后续素材按编号和角色隔离使用，禁止跨图混合人物身份、服装、产品结构、背景、姿势与调色，也不再追加会改变构图的通用自由创作规则。
- 修复填写“复刻补充说明”后视觉模型可能返回过短提示词、导致构图锁定、DWPose、资产配对、多模特一一绑定及无字规则丢失的问题；关键约束不完整时会自动回退到完整复刻基线，再以用户说明作为最高优先级覆盖。
- 一键复刻“准备资产”在识别到多位模特时，会同步生成“产品A、产品B……产品N”独立资产格，可分别为每位模特绑定不同服装；复刻请求按模特与产品槽位稳定排序，提示词强制同字母一一配对，要求可穿戴商品完整换到对应模特身上，并禁止交叉套用、互换、串穿或混搭。普通产品自动匹配只填充产品A至N，不再误占“产品细节”槽。
- 修复一键复刻“准备资产”在“两位女孩/男孩”等双人镜头中仍只生成一个模特格的问题；原帧视觉解析与 DWPose 检测人数现在会持久化并共同兜底，页面按原画面从左到右稳定生成“模特A、模特B……模特N”。复刻提示词逐位定义原图人物位置并绑定对应资产图片，禁止多人左右互换或交叉套用身份。
- “动作骨架”资产格新增移除按钮；移除后立即清空该镜头的骨架提交状态并可重新提取，只清理由应用在项目内生成的 DWPose 缓存，不会误删外部参考文件，也不会丢失已经识别的模特人数。
- 修复一键复刻“准备资产”自动匹配槽位错误：资产库中的“综合参考”图片会按名称、别名和描述在本地识别为模特、产品或产品细节，自动进入对应格；点击或拖拽模特格时也能看到并选择这类资产。
- 精简一键复刻“准备资产”逐镜解析区，只展示可勾选保留的元素；DWPose 骨架改为资产格并支持全屏缩放浏览，“重新解析”和“重新提取骨架”移到“一键替换产品”左侧。
- Windows 上传、导入和保存对话框改由应用自有原生通道在独立 STA 线程显示；即使 Explorer Shell、失效网络位置或第三方扩展长时间不返回，Flutter 平台线程仍可处理窗口消息和重绘，原有互斥、目录回退、过滤器及诊断日志保持不变。
- 进一步治理“点击上传、导入、保存后整窗像卡死”的 UI 线程风险：一键复刻、视频生成、导出、视频分析、拍摄脚本、故事设计、网格裁切、设置和故事板页面已移除全部同步文件/目录检查；缩略图与视频状态由异步缓存刷新，明确用户操作改为异步文件 API。
- 修复故事设计“添加为资产”弹窗在关闭动画期间可能提前释放输入控制器、触发界面异常的问题；输入控制器现在跟随弹窗路由真实生命周期安全释放。
- 一键复刻“准备资产”页现在会按每个镜头识别到的模特数量自动创建“模特A、模特B……模特N”资产格，并固定预留“产品”和“产品细节”格；可从右侧资产库直接拖拽或点击筛选选择，重新进入页面后仍保持槽位对应关系。
- 多模特与产品细节资产会按槽位顺序提交给分镜复刻模型，提示词逐图明确“模特A/模特B”身份和“产品细节”局部结构、接缝、材质用途，避免多人身份串位或细节图覆盖产品整体外形。
- 修复任意页面打开上传、导入或保存文件窗口时可能整窗无响应的问题：14 个文件对话框入口统一使用进程级互斥、可访问的本地初始目录和首帧刷新，避免重复打开及 Windows 恢复到失效网络盘、云盘或历史目录。
- 新增文件对话框诊断日志与 3/10/30 秒慢响应告警；日志记录入口、阶段和耗时，不记录所选文件名，便于区分 Windows Explorer/Shell 挂起与应用业务异常。
- 一键复刻“准备资产”新增本地 DWPose 高精度骨架提取：可逐镜或批量生成包含身体、面部和双手关键点的骨架参考图，并在生成复刻分镜时与原帧、新资产按明确用途共同提交，提高人物动作与姿态一致性。
- DWPose 采用 Windows 应用内 ONNX 推理，约 351 MB 官方模型直接随安装包内置，无需用户下载、部署、安装 Python 或启动外部姿态服务；准备资产页已移除“部署 DWPose”按钮，安装后可直接批量或逐镜提取骨架。
- 修复“确认镜头”右侧分镜故事面板会把项目内其他故事板的旧摘要误当成当前故事的问题；未关联故事板或关联故事板暂无摘要时现在保持空态，仅展示当前脚本明确关联的故事板摘要或用户手动输入内容。
- 修复视频解析页在访问过故事板、拍摄脚本等带拖放功能的页面后，拖入视频可能失效、必须重启软件才能恢复的问题；保活的隐藏页面现在会暂停原生拖放监听，切回当前页后自动恢复。
- 修复视频解析页移除候选帧后，已生成的视频故事板仍保留旧帧的问题；删除、撤销和重做会按稳定帧 ID 同步对应故事板，并保留其他镜头及已有人工文案。
- 原视频 IO 点预览、生成视频 IO 编辑、生成视频格子播放和全屏播放均支持空格键播放/暂停；格子播放器只响应最近点击激活的视频，避免多个成片同时播放。
- 生成视频 IO 时间轴上方的播放位置区域支持按住鼠标连续自由拖动，拖动过程实时更新视频位置，同时保留原有单击定位和下方 I/O 双端点调整。
- 设置页改为左侧功能菜单、右侧操作区的双栏布局；每次只显示当前分类，移除原有逐项折叠卡片和展开动画，较长菜单可独立滚动。
- 修复生成视频 I/O 编辑：点击时间轴上方的时间码刻度或色块轨道会立即移动播放光标并同步视频位置；设置 I/O 后，生成视频格子与作品卡片播放器会即时应用新范围并回到新 I 点，恢复完整范围也无需重新创建播放器。
- 移除生成视频页面中误导性的“达芬奇流程整合插件响应超时”红色提示：该 HTTP 超时不再被当作插件运行状态展示，未手动打开插件时仍可使用自动拉起联动；项目未打开、插件未安装、素材或同步失败等真实错误继续显示。
- Resolve 流程整合插件升级至 0.2.6：“发送到达芬奇”会在追加后立即核对每段素材的真实帧长；当 Resolve 21 Workflow Promise API 的源出点边界导致所有片段统一短 1 帧时，插件会清理本次无效写入、自动切换 inclusive/exclusive 语义并重试，消除相邻素材间的 1 帧空隙。
- 自由创作视频提示词改为按当前视频模型完全隔离生成：可灵只读取可灵图生视频规则，即梦/Seedance 只读取 Seedance 规则，MiniMax H3 不再作为其他模型的中间格式。可灵和即梦以参考图作为主体、产品、服装与颜色的事实来源，文字重点描述动作、运镜和必要约束；切换模型后必须重建该模型的原生提示词，不再跨模型截断或派生旧提示词。
- “H3 Skill 路由偏好”改为本地 H3 专属能力：仅当当前视频模型为本机回环地址上的 MiniMax H3 时显示并生效。可灵、即梦、远程 H3 和未知 HTTP 模型均不显示该入口，也不会读取或执行 H3 专项 Skill。
- “发送到达芬奇”新增 Resolve 流程整合插件自动拉起：本地桥接未运行时，Windows 会按可访问性菜单名自动调用“工作区 → 流程整合 → FilmStoryboard 时间线桥接”，等待连接后继续发送，不再要求用户手动逐级点击。插件升级至 0.2.5，关闭状态窗口后桥接会在 Resolve 会话内继续驻留；Resolve 未运行、插件未安装或权限不一致时会返回具体指引。
- 修复连续镜头组在确认镜头页完成构建后，进入生成视频页只剩默认参考句、完整可灵提示词被丢弃的问题；“图片1至图片N作为/为……”形式的多图可灵提示词现在会原样进入生成草稿。
- Resolve 流程整合插件升级至 0.2.4：时间线快照现在以 FFprobe 解析到的视频流时长和真实源帧数收紧 I/O 出点，调用 Resolve `AppendToTimeline` 前再次限制末帧，避免相邻素材间出现 1 帧空白；受管时间线的起点与时长改为严格帧级校验，不再容许 1 帧偏差被误判为无变化。
- 修复确认镜头页“构建脚本”的全局构建与单镜头构建结果未进入下一步视频生成提示词框的问题：视频生成草稿现在直接监听已完成的构建结果，不再只依赖拍摄脚本字段的二次回写；单镜头重构只刷新目标镜头，其他镜头提示词保持不变。
- Resolve 流程整合插件升级至 0.2.3：修复 Resolve 21 新时间线必须先启用 `useCustomSettings` 才能修改分辨率和帧率的问题；时间线成功保存后不再因 `GetUniqueId()` 的 Promise API 兼容异常被误删，并增加受管绑定名称回退。插件会把真实失败阶段和原因返回软件及插件状态页。本机 Resolve 21.0.0.47 已完成创建、自动打开、素材写入和重复同步不覆盖的宿主验证。
- 修复 `1.0.0.280` 已越过脚本解析后仍因 `manifest.xml` 被 Windows PowerShell 5.1 按 ANSI/GBK 误读而退出的问题：安装脚本改为显式 UTF-8 读取清单，并用安装包真实的无 BOM 中文清单完成回归；安装器和软件设置页还会优先展示 PowerShell 写入的真实错误日志，不再只显示通用提示。
- 修复安装包和软件“设置 → 插件”在 Windows PowerShell 5.1 中解析 Resolve 部署脚本失败的问题：安装、卸载与回归脚本统一保存为 UTF-8 BOM，并新增编码断言及 PowerShell 5.1 实际运行验证。
- Resolve 流程整合插件升级至 0.2.2：安装器与设置页现在直接把软件 `data/resolve_plugin` 中的内置插件包复制到官方插件目录，不再因 SDK 缺失、Resolve 正在运行、版本或版本类型而拒绝部署；安装成功只代表文件复制完成，插件能否加载由目标机 Resolve 环境决定。若检测到官方 SDK 原生模块仍会顺带复制，但不是安装前置条件。
- 构建脚本与自由创作新增严格的播放速度授权：只有当前镜头“剧情描述”明确写出慢动作、慢放或升格时才允许使用；缓慢运镜不再被误解为慢动作。导演语法统一以项目内置的即梦 / Seedance 2.0 官方提示词规则为基线，按空间层与时间层组织主体、动作、表情和运镜；单图设计一条有动机的主运镜，多帧默认视为同一镜头的连续阶段。
- Web“生成视频”页在宽屏下新增固定右侧“作品管理”面板，生成提交、镜头任务、失败重试和镜头版本可在编辑镜头时同步查看；窄屏改为可展开的单列入口。
- 完成视频解析、故事板、拍摄脚本、生成视频和导出五页跨层联调，页面切换后继续保持当前视频、候选帧、故事板素材和脚本状态，浏览器仍只调用本机白名单 API。
- 安装包内 Web 静态资源已重新同步并固定使用随包 CanvasKit、中文字体与 fallback 字体，不依赖第三方业务 API、远程字体或本机绝对路径。
- “确认镜头”页将“可灵 / H3 / 即梦”提示词切换项从每个脚本条目提升到提示词列表头；顶部一次切换即可同步全部镜头，行内空间只保留提示词编辑与操作。
- 视频生成页的成片 I/O 裁剪按钮移至视频下方，并与原视频“按 IO 点预览”统一为左对齐描边按钮；裁剪状态和区间时长现在无需遮挡画面即可查看。
- “确认镜头”功能菜单新增单镜头/镜头组“解析”图标，修改某个镜头后可只重新构建当前组，不再被迫全盘构建。
- 重复构建会从仓库重新读取最新复刻记录；镜头组只有在复刻图完整时才整组使用复刻分镜，否则整组使用原视频帧，不再混合提交，并记录实际图源与路径供核验。
- 修复脚本重建后生成视频页仍停留旧“手工稿”的问题：页面会自动展示新构建稿，同时保留旧手工稿供用户主动切回。
- 生成视频支持对每个本地成片设置并持久化 I/O 范围：格子和作品管理大预览均可通过时间码时间轴拖拽入点/出点，播放器只播放选中区间，左右键切换相邻镜头、Esc 退出大预览。
- 时间线 XML 现按镜号、最新可用版本和 I/O 范围导出；生成页新增“导出视频”，可将各镜头裁剪后统一画幅、帧率和音轨并拼接为 H.264/AAC MP4。
- 作品管理标题区新增“清理”，可移除当前脚本中本地文件已丢失的完成作品记录，并同步刷新预览状态。
- Web 导演工作台已贯通视频解析、故事板、拍摄脚本、生成视频和导出五个工作页；刷新浏览器可恢复本机任务，页面通过 WebSocket 与桌面事实状态同步。
- Web“导出”支持故事板 PNG/JPG/PDF、画板原图、拍摄脚本 XLSX、视频解析报告和剪辑时间线 XML；全部文件由本机 API 生成，支持取消、失败重试、安全预览和下载。
- 浏览器不接触 API Key、本机绝对路径或第三方结果直链；导出产物只通过当前工程受控目录内的随机 ID 访问，切换工程后自动失效。
- 视频解析、故事板、拍摄脚本和视频生成页面统一按素材真实画幅完整显示；项目自动画幅扩展为 16:9、4:3、3:4、4:5、9:16，竖屏及其他非 16:9 素材不再被压入横屏框或裁切。
- “确认镜头”表格在原始视频帧范围左侧新增镜号列，设置首帧和结束帧后会同步显示连续镜号及镜头范围。
- 拍摄脚本流程调整为“1 准备资产、2 确认镜头、3 生成视频”，新建任务默认从准备资产开始，旧任务仍可安全恢复。
- 故事板支持外部图片拖入：拖到裁切资源面板或具体文件夹时只保存资源；拖到画板空白区域时会归档到“手动添加”并自动加入画板排版。
- 修复 LibTV CLI 缺少生成参数选项的问题：视频生成页现可设置自动或六种固定比例、480p/720p 清晰度、生成音频与联网增强；每个镜头仍可独立编辑 4–15 秒时长。所有选择会按脚本持久化，并真实传入 LibTV 官方 `node create --set` 参数，联网增强不再被固定开启。
- 可灵与 LibTV CLI 新增软件内自动安装：检测缺失后会在视频生成页提示安装；可灵先检查 Node.js 18+ 与 npm，必要时通过 winget 安装 Node LTS，再按用户明确选择的中国区或海外区安装对应官方 npm 包；LibTV 直接运行安装包内置的官方 PowerShell 脚本且不依赖 npm。安装完成后会自动重新检测并打开浏览器授权，账号授权仍需用户本人确认。
- 视觉模型的内置 Skill 注入改为两级按需路由：先以设置页当前选中的视频生成模型做硬门控，可灵只读取可灵 Skill、LibTV/Seedance 只读取 LibTV Skill、MiniMax H3 只读取 H3 规则；未知 HTTP 视频模型不再被默认当作 H3，避免无关 Skill 污染提示词。
- MiniMax H3 在“自动匹配（通用 H3）”模式下，会分别读取每个镜头或镜头组的剧情描述、画面内容、复刻要求与生成反馈，在 3D 动画、品牌宣传、双人游戏开场、手绘实拍、极简产品广告、音乐 MV、纸拼贴讲解、纸艺定格科普中至多选择一个专项 Skill；没有明确特征时只使用通用 H3。手动选择专项风格仍可作为明确覆盖。
- 脚本构建与自由创作不再为整批镜头共用同一份完整 Skill 上下文；路由在本地确定性完成，不增加视觉 API 请求。自由创作会把实际后端、命中 Skill、自动选择状态和视频模型写入输入指纹，切换模型或剧情后不会错误复用旧提示词。脚本分析规则升级至 6，自由创作提示词规则升级至 22。
- 视频生成新增内置 `LibTV CLI · 即梦 2.0` 预设：首次使用会自动调用 LibTV 浏览器授权，登录完成后自动读取账号与 Seedance 2.0 模型；镜头参考图会上传到按脚本复用的独立 LibTV 画布，图片编号按即梦规则转换为画布节点引用，生成过程同步等待 CLI 终态并下载结果，不通过外部轮询重复查询。
- LibTV CLI、可灵 CLI 和 MiniMax H3 的完整 Skill 资源现已随软件安装包内置。用户接入任意受支持的视觉 API 后，“构建脚本”和自由创作会按当前视频后端读取对应 Skill；LibTV 同时读取即梦 2.0 官方提示词指南，CLI 的登录、上传、扣费提交和下载仍由软件安全执行。
- 设置页补齐 LibTV CLI 卡片、三类后端编辑入口与即梦 2.0 官方提示词教程；生成页展示 CLI 版本、账号、模型与真实参数，并在提交前明确 4–15 秒、720p、有声、顺序执行和脚本专属画布规则。可灵 CLI 与现有 HTTP API 行为保持不变。
- “确认镜头”自由创作表格在“提示词”右侧新增独立“功能菜单”列；每个镜头组可通过行尾手柄整组拖拽排序，也可经二次确认后整组移除。排序会同步镜号、提示词和复刻图记录，移除会清理关联提示词与数据库记录，但不会删除已生成的本地文件。
- MiniMax H3 本地视频模型的分辨率下拉改为启动及切换 API 时读取本地服务 `/api/config`，自动同步后端新增的全部分辨率、比例和默认值；接口暂不可用或返回无效列表时继续使用内置分辨率，避免阻断视频生成。
- 设置页新增“解析维度”，可独立勾选“多维度分析”和“镜头明细”。未勾选的类别不会发起对应视觉解析请求，也不会出现在 PDF、XLSX、PNG、JPG 多维度报告中；仅开启多维度时会把最多 12 张时间均匀候选帧合并为一次多图请求，以减少等待时间。
- 视频添加并完成候选帧抽取后，会立即自动创建对应故事板画板和拍摄脚本，不再等待整段视频视觉解析完成；视频解析页只要存在候选帧，也可随时手动生成或恢复故事板。
- 后台视频解析完成后会复用同一画板、主脚本和源帧镜头原位补充结果，不会重复创建；用户在解析期间已填写的画板文案、脚本内容和摄影字段会优先保留，避免后台回填覆盖正在进行的编辑。
- 拍摄脚本页允许在未填写任何文字描述时直接点击“构建脚本”：系统会仅依据参考图自动分析主体、动作、镜头节奏、运镜、声音与合适的创作方向，不再用必填校验阻断创作。
- 创作流程由“确认镜头 / 准备资产 / 合成提示词 / 生成视频”精简为三步；可灵、H3、即梦提示词选择器已迁移到确认镜头的提示词列。切换视频 API 或模型时会自动选择并显示对应类型，旧工程保存的“合成提示词”步骤会安全恢复到确认镜头。
- 每次点击“构建脚本”都会先清空上一轮提示词的页面状态、脚本字段和数据库记录，再生成并保存全新的可灵、H3、即梦三种版本，避免旧提示词混入本次结果。
- 故事板画布图片现在可直接拖入左侧“裁切资源”的自定义文件夹；拖到目标文件夹时会显示主色微光和柔和阴影，释放后复制单张或多选图片到对应文件夹，同时保留画布中的原图片与既有排序行为。
- 修复拍摄脚本重新调整首帧和结束帧范围后，重复点击“构建脚本”仍复用第一次范围提示词的问题。用户手改提示词只在镜头组范围与当前输入完全一致时保留；范围从 1-3 改为 1-2 后会按当前 1-2 重新构建，拆出的镜头也会独立生成。
- 自由创作提示词输入指纹新增镜头组 ID 和镜头编号，规则版本升级至 21；即使不同镜头引用相同图片，也能正确识别首尾帧范围变化并使旧缓存失效。
- 顶层功能页改为首次访问后常驻保活：切换页面不再销毁页面 State，滚动位置、输入内容、面板展开状态与后台监听会继续保留。
- 拍摄脚本构建、视频解析与故事板视觉任务改为按业务实体独立运行：不同脚本、视频和故事板可并行，同一实体继续防重并保持必要的串行顺序；切换后会恢复各自进度和消息。
- 故事板自动解析与自动重排序采用“同一画板串行、不同画板并行”的独立通道；取消当前画板只进行逻辑取消，不再调用共享视觉客户端的全局取消，因此不会误伤其他画板任务。
- 故事板图片修改与高清重绘的忙碌状态改为按画板隔离；后台结果始终写回任务启动时的画板与格位，A 画板完成不会清除 B 画板状态，也不会抢占当前画板或覆盖当前消息。
- 修复视频生成页在当前页面内点击“重新生成”或“一键生成全部”时，部分镜头只停留在等待状态、必须切换功能页才能继续的问题。现在按镜头独立防重：正在运行的同一镜头不会重复扣费提交，其他镜头及已失败镜头可在原页面立即提交；一键生成会自动跳过已运行镜头，批次异常也不再留下永久锁死的草稿任务。
- 修复“构建脚本”完成后镜头组时长全部变为 0 的问题：单镜头和多帧组会依据画面动作、状态变化、运镜复杂度与参考帧数量生成 3–12 秒的合理默认值；普通镜头组总时长统一写入并读取组尾，确认页、生成页和实际任务不再出现组首/组尾分裂。
- 视频生成工作区由四列扩展为五列，新增可编辑“时长”列。用户可保留智能默认值或手动输入；输入会按当前后端支持范围归一，并同时更新页面时长、提示词中的“X秒视频”、视频任务 `durationSeconds` 和真实 HTTP API/可灵 CLI 的 `duration` 参数。
- 构建脚本不再要求视频按具体每秒执行动作：即使所选完整官方 Skill 含逐秒模板，当前镜头也只保留自然的“开头、随后、接着、最后”事件顺序和一次总时长；视觉模型返回的“第 N 秒”或“0-N 秒”标签会被清理。提示词规则升级至 19，旧缓存会自动进入待重新合成状态。
- 严重修复“确认镜头”构建脚本后切换到其他页面，返回时已构建状态被重置、“生成反馈”输入列消失的问题；提示词因资产或替换要求变更而进入待重拼状态时，已构建稿仍会持续显示并可填写反馈。
- 准备资产页手动“一键替换产品”改为即时并发提交：同一脚本的前一个复刻任务尚未结束时，其他镜头点击后会立即进入视觉分析和图片生成 API，不再被脚本级单任务锁静默拦截；页面忙碌状态会保留到全部并发任务结束。
- 修复“确认镜头”已构建视图中“生成反馈”文本框输入异常卡顿的问题：连续输入先保留在控件本地，停止输入或离开文本框后再合并保存一次，不再逐字同步替换整份镜头列表并刷新页面；反馈仍会自动持久化并正常触发局部重构。
- “构建脚本”彻底移除自动镜头划分：视频导入、视觉解析、构建和后续镜头组展示都不再根据场景、动作或模型结果自动补连。镜头范围完全以用户手动设置的首帧和结束帧为准；同一范围内的首帧、中间帧和结束帧按时间顺序合并进同一个视觉 API 请求，未设置范围的帧各自独立。视觉模型返回的连续性字段不能再覆盖手动分组。提示词规则升级至 18，旧分组缓存会自动失效。
- 修复普通多帧镜头组在拍摄脚本显示 4 秒、生成任务却只提交组首 1 秒的问题；关闭首尾帧模式时，实际 API/CLI 时长和 H3 提示词现统一使用拍摄脚本显示的组尾时长，显式首尾帧模式仍按连续片段累加。提示词规则升级至 17，旧的错误时长缓存会自动失效。
- 软件已内置 MiniMax-H3 的 8 个官方中文 `SKILL.cn.md` 及其全部必要引用文档，共 15 个上游原始文件，不再打包或加载重复的英文 `SKILL.md`；选择叙事风格后，“构建脚本”会让视觉模型读取对应完整中文正文，再按完整步骤、门禁、模板、画面材质、叙事结构、镜头节奏、动作连续性、声音策略和质检规则解析镜头。资源缺失时会中止并报错，不再静默退回泛化摘要。提示词规则升级至 16。
- 叙事风格选择器新增官方 Skill 来源和完整读取状态；最终 H3 提示词仍叠加所选风格的成片锁，避免完整 Skill 在视觉分析后的本地拼接阶段被弱化。
- “确认镜头”的“构建脚本”现在按视觉 API 额度并发处理不同镜头组，大幅减少多镜头脚本的等待时间；同一镜头组的多张图仍按时间顺序合并到一次多模态 API 请求中，不会拆分成多次解析。
- 恢复“匹配资产图”步骤右侧资产库：重新显示按类型分组的资产缩略图卡片与管理入口；“上传资产”和“按描述生成”统一移入资产管理页面，点击“自动匹配资产”会继续按名称/别名将资产库内容绑定到对应镜头。
- 视频生成页现在会在打开工程时扫描已完成成片：如果用户在文件夹中手动删除了实际 MP4，对应的视频生成和作品管理记录会自动移除，不再显示失效占位。扫描仅作用于已完成/部分完成记录，失败、取消、排队和生成中的任务会保留。
- 修复取消视频生成后任务状态被后台恢复查询重新写成“生成中”的问题：取消标记现会传递给普通生成、手动继续查询、启动恢复和超时恢复流程；任何延迟返回的旧状态都不能覆盖已取消任务。已取消任务不再反复显示“视频生成中”。
- 修复视频生成页“生成提示词”输入框无法连续键入的问题：草稿每次保存不再销毁重建输入控件；现在支持正常的中英文直接输入、中文输入法组合和粘贴，外部切换提示词版本时仍会立即同步文本。
- 按可灵官方多图参考与 O3/3.0 Omni 用法重写可灵提示词：参考图负责主体、服装、产品和场景一致性，正文只保留连续动作、核心运镜、必要环境与用户明确约束；普通多帧不再输出内部镜号、逐段秒数、摄影分析证据和重复声音规则，自动提示词控制在 500 字以内。提交阶段只补缺失的资产编号，不再复述服装细节和长篇一致性禁令。提示词规则升级至 15。
- H3 参考素材说明不再输出“依次对应镜头4至6”或“图片2是镜头5的动作参考”等仅应用内部可理解的拍摄脚本镜号；视频模型现在只接收图片编号、顺序用途和连续性要求。提示词规则升级至 14。
- H3 合成提示词不再自动拆分“0–1.7秒、1.7–3.3秒”等阶段时间区间；单镜头使用自然画面描述，多帧使用“开头、随后、接着、最后”的事件顺序，只保留一次视频总时长，也不会凭空添加 Logo 或其他用户未输入的精确时间事件。提示词规则升级至 13。
- 重写 MiniMax H3 本地提示词编译规则：普通多帧镜头组改为一条顺序动作参考，场景、运镜、光影、色彩和焦点只在组级描述一次；各时间段仅保留动作变化，不再输出多帧证据、原始运镜、摄影备注和重复声音段落。
- 修复 H3 首尾帧提示词与真实 API 模式不一致的问题：只有双帧且无额外资产时使用精确首尾帧语义；含中间帧或资产时统一使用多参考图语义。
- 修复结构化分析中的服装/道具在清洗后被重新注入，以及视频提交前再次拼接整段参考素材说明的问题；现在先合并后清洗，并且只补提示词中缺失的资产编号。提示词规则升级至 12。
- 修复 Windows 下可灵 CLI 提交较长中文提示词时立即出现 `FormatException: Missing extension byte` 的问题；应用现在绕过 `cmd.exe` 直接启动可灵 Node CLI，并能兼容解码本地控制台错误信息。
- 修复“根据反馈重构”成功后反馈数据虽已清空、输入框却仍显示旧文字的问题；反馈框现在会立即同步为空，同时保持正常输入时的光标和中文输入法状态。
- 修复反馈重构后“合成提示词”页没有自动补齐最终提示词的问题：反馈镜头重新拼接，未反馈镜头沿用原结果，缺失或失败的旧提示词会自动补齐，并按完整合并镜头组一次性写回。
- 合成提示词完成数和结构化解析统计统一按合并后的镜头组计算；17 个原分镜合并为 6 组时显示 6/6，不再显示容易误解的 6/17。
- “确认镜头”首稿完成后，在“画面描述”右侧新增可持久化的“生成反馈”列；用户填写需要纠正的问题后再次构建，只会让视觉模型重新设计有反馈的镜头组，不会全盘重构。反馈会结合上一稿画面和运镜作为强约束，成功后自动清空；对应最终合成提示词局部更新，未反馈镜头及其现有提示词保持不变。
- 已生成首稿的工程重新进入确认镜头页时，会恢复已构建视图并继续显示“生成反馈”，无需重新全量构建。
- 视频生成页右侧“作品管理”面板新增作品右键菜单：可在资源管理器中打开并选中实际 MP4，可另存为新路径，也可经二次确认后同步删除本地视频与对应任务记录。
- 新生成视频的实际保存文件名改为按合并后镜头组连续编号，例如 `镜头1-v18.mp4`、`镜头2-v11.mp4`；不再沿用原分镜组首的 1、4、7 跳号。该规则只作用于升级后新生成的成片，已经生成的旧文件不会被重命名、迁移或覆盖；时间线继续引用旧文件的真实路径。
- 确认镜头页按合并后顺序显示镜号，并用括号保留原分镜范围提示，例如 `1（1-3）`、`2（4-6）`、`4（10）`。
- 最终合成提示词改为保留最后一次有效结果：切换页面、刷新、重启软件，或镜头、风格、素材与首尾帧变化后都不再清空；需更新时只标记为待重新合成，新结果完成后再替换。
- 修复合并镜头组导出时间线时仍带入组内历史单镜头视频的问题：导出现在与拍摄脚本分组完全一致，每个合并组只取组首关联的最新本地成片，例如 17 个原始镜头合并为 6 组后，时间线只会包含 6 条成片。
- 修复 Premiere 导入 XML 时本地视频全部离线的问题：Windows 媒体 `pathurl` 改为 Premiere 手动链接后使用的 `file://localhost/D%3A/...` 形式，中文与空格路径继续正确编码，导入时不再解析为错误的 `\\\D:\...`。
- 严重修复普通多帧镜头组的视频时长被逐帧累加并统一截成 15 秒的问题：关闭首尾帧模式时，多张图片只作为同一条视频内的动作阶段参考，实际 API/CLI 提交时长使用构建后镜头组自身时长；H3 与可灵提示词的阶段时间也会归一到该总时长。只有显式开启首尾帧模式时才继续累加连续动作组时长。提示词规则升级至 11，旧的错误时长提示词缓存会自动失效。
- 确认镜头步骤点击“构建脚本”并成功完成全部镜头组后，会立即在本地自动拼接全部视频提示词；流程仍停留在确认镜头，不会跳过准备资产步骤。之后进入“合成提示词”页面即可直接检查结果，原按钮保留为重新拼接入口；构建取消或部分失败时不会误用旧字段自动拼接。
- 修复普通手动镜头组被误判为首尾帧模式的问题：关闭首尾帧开关时，组内图片按镜头顺序作为普通多参考图，不再出现首帧、尾帧或两图插值措辞，也不会提交 `last_frame`；只有显式开启首尾帧开关才使用首尾帧语义。提示词规则升级至 10，旧错误缓存会自动失效。
- 修复确认镜头页手动镜头组未传递到合成提示词和视频 API 的问题：原视频帧组或复刻分镜图组会按镜头顺序完整作为参考图，并汇总组内全部已确认资产；H3 图片编号从组内帧之后继续递增，提示词编号与 multipart 上传顺序保持一致。
- 拍摄脚本音效解析改为按画面可见动作、道具接触与环境事件生成具体声音，并要求每个脚步、触碰、开合、碰撞和摩擦声以真实时间速度逐次同步；禁止慢放、时间拉伸、低沉变调、拖长尾音和过长混响。未明确要求时不再自动添加非叙事性背景音乐。
- 镜头编组提交视频生成时，会按时间顺序上传全部复刻分镜图（缺失时回退原视频帧）并汇总组内所有镜头已确认的资产图；重复资产自动去重，`<Picture N>` 与 multipart 文件顺序保持一致，不再只上传首镜头关联素材。
- 视觉解析固定在“构建脚本”阶段执行：普通脚本条目优先提交复刻分镜图，没有复刻图时才回退该条原视频帧；首尾帧镜头组优先使用完整复刻分镜组，只要复刻组不完整就整组回退对应原视频帧组，禁止复刻图与原帧混合解析。组内多帧按时间顺序合并为一次视觉 API 请求。
- “构建脚本”升级为导演式镜头规划：先区分参考画面可证实的原始运动与最终创意运镜，再结合全局故事、镜头位置和所选内容风格设计起势、路径、幅度、速度曲线与视觉落点。
- 镜头叙事风格选择前置到构建阶段；品牌宣传、极简产品广告、剧情、MV、纸艺等使用不同镜头语法，并在镜头组综合时读取已有相邻运镜，避免全片重复固定、轻推或同方向移动。
- 构建结果新增“导演运镜、构图/机位、焦点/衔接、摄影备注”结构化列；固定机位只能作为有明确画内调度和叙事目的的主动设计，不再作为证据不足时的批量默认值。
- “合成提示词”改为读取构建脚本字段并在本地确定性拼接 SD2、可灵和 H3，批量合成、单条重生成及失败重试的视觉模型调用均为 0；提示词规则版本升级至 9。
- 本地 H3 视频生成补齐 `21:9`、`16:9`、`4:3`、`1:1`、`3:4`、`9:16` 六种官方比例，下拉、持久化、HTTP 提交与本地服务白名单已贯通。
- SD2、可灵和 H3 提示词统一接入结构化事实，保留用户手工字段优先级；解析上下文、素材描述、全局规则或相邻镜头变化后会自动使旧提示词失效。
- 新增 50 镜头本地批量合成性能回归，并完善 Schema 17 跨版本数据库迁移兼容。
- 本地合成提示词保留叙事驱动的动态运镜规则：结构化解析事实会结合主体动作、画面空间、全局故事调性和前后镜衔接生成可执行运镜，写清起势、速度曲线与焦点变化；不再把慢推、慢拉、轻微推近或慢动作当作默认电影感。
- 修复视频生成页原视频 IO 点预览：播放器加载时原生锁定 I/O 区间，避免所有镜号都从视频开头播放；多帧画面合并为一个镜头时，精确播放首帧到末帧的时间区间；未合并的单帧继续按设置的前后秒数预览。
- 导出页的多维度报告入口固定显示“导出多维度报告”，不再拼接冗长的视频文件名。
- 默认导出根目录新增“故事板、画板图片、拍摄脚本、时间线、多维度报告”分类文件夹；“导出到...”仍保留自选路径能力。
- 确认镜头右侧“分镜故事”改用细线分隔，点击镜头描述可自动滚动定位到左侧对应分镜行。
- 修复本地 H3/MiniMax 视频 API 任务已生成完成但软件仍显示 `timedOut` 的问题；进入视频生成页后会后台继续查询历史超时任务，不会重新提交或重复扣费。
- 本地视频 API 查询会在任务接口未返回完成视频时继续读取 `/api/works` 作品库，命中同一 `generationId` 后自动下载并同步回对应分镜镜头。
- 修复当前工程生成视频列的显示路径解析；已完成视频无论保存为绝对路径还是工程相对路径，都能正确出现在“生成视频”列中。
- H3 本地拼接会完整保留构建阶段确认的场景、动作、构图、机位、运镜、摄影备注、焦点路径、剪辑衔接、声音与音乐，不再在合成阶段重新解释故事。
- 提示词版本入口统一为“可灵 / H3 / 即梦”，视频生成页为“可灵 / H3 / 即梦 / 手工稿”，方便直接对比不同模型规则。
- 视频生成页未生成视频时，生成视频格子内直接显示“生成视频”按钮，不再收纳到右下角圆形菜单。
- 可灵 3.0 Omni 图生视频提示词改为按官方 `图片1`、`图片2` 输入编号引用，普通首帧和首尾帧生成都能正确指向素材。
- 首尾帧动作组提示词改为镜头时序结构，保留中间动作过程，并明确从 `图片1` 自然过渡到 `图片2`。
- 视频生成提交前会读取可灵模型输入能力；当前模型不支持尾帧输入时会阻止首尾帧提交，避免无效扣费请求。
- “自动解析全部”只更新画面、动作、运镜等脚本字段，不再写回或补齐任何连续动作布尔字段；镜头组端点统一由用户手动设置。
- 首尾帧模式下，可灵提示词会保留中间动作过程；生成视频可只使用首帧和尾帧，中间帧作为动作描述写入提示词。
- 合成提示词默认选中并同步“可灵”，即梦规则版仍保留并可手动切换。
- 首尾帧模式下，“确认镜头”和“合成提示词”表格会显示首帧、尾帧、复刻首帧和复刻尾帧；一键复刻会为连续动作组生成首尾两张端点复刻图。
- “合成提示词”按钮文案改为基于已解析脚本字段合成，避免误以为会在此步骤重新调用视觉模型。
- 一键复刻分镜默认执行“纯净无字画面”硬约束：原视频帧及参考素材中的字幕、文字、数字、Logo、水印、商标、台标、角标、二维码和条形码均不得复制或重绘，并以自然无标识细节重建。
- 只有用户在单镜头“复刻补充说明”中明确给出需要逐字出现的具体文本时，才允许生成该段指定文本；参考图中的其他文字和 Logo 仍会被禁止。
- 视频生成工作区的“原视频、复刻分镜图、生成视频”三列统一显示镜头编号，标题基线和媒体顶部水平对齐。
- 视频解析和拍摄脚本解析改为结合前一帧、当前帧与后一帧判断人物动作阶段和运动趋势，减少对单帧画面的主观猜测。
- “确认镜头”右上角新增可持久化的实验性“首尾帧模式”；双向确认连续且场景不冲突的动作镜头会合并为一条首尾帧生成请求。
- 可灵图生视频提交支持真实尾帧参数，批量生成、时长和付费确认均按连续动作组计算。
- “生成视频”格支持直接播放、暂停和应用内全屏，生成与管理操作收纳到格子右下角悬浮菜单。
- 视频生成页原视频缩略图点击后，弹窗会自动从 I 点开始播放，无需再次点击播放按钮。
- 视频生成页“复刻分镜图”列支持点击进入全屏浏览，可缩放并切换其他可用镜头。
- 修复 Windows 下可灵 CLI 中文账号信息按系统代码页解码导致的顶部文字乱码。
- 首次进入视频生成页、切换到视频生成步骤或切换拍摄脚本时，会自动加载默认图生视频参数：可灵 o3、16:9、1080p；已保存的有效用户选择不会被覆盖。
- 修复可灵视频下载完成后 Windows 拒绝将 `.mp4.part` 重命名为最终文件的问题；写入句柄会在落盘前显式刷新并关闭，重命名失败时自动回退复制。
- 生成视频文件名统一为“镜头号+版本号”，例如 `镜头005-v1.mp4`，并自动迁移历史超长生成目录配置。
- 原视频列新增视频帧缩略图，点击缩略图即可弹窗播放对应 I/O 区间。
- 设置页新增“播放视频帧前后（秒）”，默认 1.5 秒，可在 0.1–30 秒范围内自定义并持久化。
- 可灵下载失败提示新增官方 24 小时结果 URL 有效期说明，提醒及时保存。
- 修复视频生成页误报“原视频不可用”：源帧关联缺失时会按帧路径和镜号恢复定位，缓存视频缺失时回退原始视频文件。
- 修复可灵视频已生成但因 Windows 超长目录导致下载失败；输出目录改用受限长度，并支持直接“重新下载”既有结果而不重复付费生成。
- “合成提示词”步骤改为“原图 / 复刻分镜图 / 生成提示词”三列；镜头信息变化后提示词列恢复为空，等待用户主动合成。
- 每个镜头会同时生成即梦规则版和可灵提示词，支持切换、编辑、复制，并将选中版本同步给视频生成。
- “确认镜头”的自动解析会补齐音效设计、建议时长和运镜；对白继续完全由用户手动填写，不会被解析覆盖。
- 新增可灵中国区一键视频生成：复刻流程扩展为四步，并在“拍摄脚本”和“导出”之间新增独立“视频生成”页面。
- 视频生成支持动态模型/参数、可灵与即梦原提示词切换、单镜头/批量付费确认、最多两路提交、无水印优先下载和历史版本管理。
- 原视频参考改为复用源文件的 3 秒 IO 点预览，不截取、不转码、不产生额外视频片段，避免工程磁盘占用膨胀。
- 可灵任务失败、取消或 15 分钟超时不会自动重新扣费提交；软件重启后只恢复查询已有 generationId。
- 一键复刻的“准备资产”页新增复刻补充说明；提交前会自动清理与用户要求冲突的提示词，并以用户要求为准生成新分镜。
- 支持明确移除原人物的耳环、眼镜、帽子、包、手表、发饰等配饰，避免将原人物外观完整复刻到新分镜中。
- 修复拍摄脚本表格长文本未充分使用单元格高度的问题。
- 修复准备资产页“全部折叠”后仍显示空白面板和逐镜箭头的问题；切换页面返回后会恢复可用的紧凑资产行。
- 修复一键复刻进行中切换页面返回后，进度状态可能被脚本同步刷新提前清除的问题。
- 多维度分析已移除“商业创意规则（ABCD + Hook-Body-Close）”分组，不再对其生成解析结果。
- 多维度分析结果按“证据、商业作用、优化建议”分行展示，阅读重点更清晰。
- 拍摄脚本资产自动绑定改为本地受控名称/别名匹配，不再上传镜头或资产图片调用视觉模型。
- 资产库支持维护匹配别名；重名资产不会自动绑定，避免错误关联。
- 删除视频自动故事板或其资源组时，保留视频解析页面的源视频帧；关联的自动拍摄脚本会同步移除。
- 视频解析页面的“生成故事板”现在会同时生成或恢复选定视频的故事板和拍摄脚本。
- 新建故事板描述默认字号调整为 12。

> Windows 安装包当前未使用公共代码签名证书，部分设备可能显示 SmartScreen 提示。
# 1.0.0.265

- LibTV 视频生成页新增实时模型下拉框，模型清单来自官方 CLI 的 `model search --type video`。
- 切换 LibTV 模型后自动读取该模型完整 schema，并动态显示生成模式、基础设置和高级设置。
- LibTV 生成参数改为按所选模型 schema 提交，支持枚举、开关、数值范围、按模式参数和模型时长限制。
- 修复任务提交仍使用设置页固定模型、`modeType=mixed2video` 及 Seedance 专属参数的问题。

# 1.0.0.266

- 新增视频朝向自动检测：同时解析 FFprobe Display Matrix 和 `rotate` 标签，正确识别 90°/270° 旋转的手机竖屏视频。
- 视频分析、原视频区间预览和生成历史播放器改为按实际显示比例自适应，使用完整显示而不裁剪画面。
- 新增项目级“自动 / 16:9 横屏 / 9:16 竖屏”画幅模式；新建工程默认由首个有效视频判定，后续相反朝向视频会明确提示冲突。
- 故事板格位、画板高度、缩略图、PNG/PDF 导出、图片生成、高清重绘与视频生成默认比例全部跟随项目画幅。
- 旧工程继续保持 16:9 布局以避免画板突变；已导入的旧视频会在后台一次性重新探测旋转元数据，且不阻断工程启动。

# 1.0.0.267

- 准备资产页在“自动匹配资产”右侧新增“生成参数”按钮。
- 生成参数弹窗支持设置比例、分辨率和质量，下拉选项与设计分镜页使用同一套图片模型目录。
- 切换比例后会自动更新当前模型支持的分辨率；不支持质量选项的模型保持禁用状态。
- 三项选择随当前复刻任务持久化，重新进入页面仍会恢复；单镜头复刻与批量一键复刻都会向图片生成 API 提交该组参数。

# 1.0.0.268

- Web 导演工作台完成五工作页本机 API 联动、实时恢复和响应式布局。
- 新增安全 Web 导出页面，支持五类真实导出、任务取消/重试及受鉴权产物预览下载。
- 多页面视频帧与播放器按 16:9、4:3、3:4、4:5、9:16 等真实画幅自适应完整显示。
- 确认镜头表新增镜号列与连续首尾帧范围。
- 拍摄脚本流程重排为准备资产、确认镜头、生成视频。
- 故事板新增外部图片资源归档与画板自动排版拖拽入口。

# 1.0.0.269

- 生成视频与作品管理新增持久化 I/O 时间轴、范围播放和键盘镜头预览。
- XML 时间线和快捷 MP4 拼接统一采用镜号顺序、最新可用版本及已选 I/O 范围。
- 作品管理新增失效视频清理入口。

# 1.0.0.270

- 确认镜头新增当前镜头组局部解析入口。
- 重复构建改为最新复刻记录优先、镜头组图源整组二选一，并保存可审计图源信息。
- 生成视频页在源提示词变化后自动展示新构建稿，同时保留旧手工稿。

# 1.0.0.275

- 新增 DaVinci Resolve“流程整合”时间线桥接插件 0.2.0，可将生成素材、I/O 点和时间线落点同步到受管时间线，并在重复同步时保护用户自建轨道。
- Windows 安装包会自动部署或卸载插件；`WorkflowIntegration.node` 始终从目标机匹配版本的 Resolve 官方 SDK Promise 示例复制，安装包与仓库不携带机器相关二进制。
- 插件安装、覆盖升级和卸载均可重复执行；SDK 缺失、Resolve 未关闭、同名未绑定时间线、受管轨道被修改或素材指纹变化时会安全停止。
- 不同 Resolve 环境的流程整合菜单可用性以目标机为准；未显示菜单时仍可使用 XML 时间线导出，宿主内加载与真实时间线 E2E 需按插件 README 的验收清单复核。

# 1.0.0.276

- 软件安装向导新增“是否安装达芬奇插件？”独立选择页，可立即安装或暂时跳过。
- Resolve 插件 0.2.1 的跨机器源码与部署脚本固定集成在软件 `data/resolve_plugin`，即使首次安装时跳过插件，后续也无需重新下载安装包。
- 设置页新增可展开“插件”项和“安装达芬奇插件”按钮，可自动请求 Windows 管理员权限并部署到 Resolve“流程整合”插件目录。
- 应用内补安装仍只从目标机 Resolve 官方 SDK 复制匹配的 `WorkflowIntegration.node`；软件 data、安装包源码载荷和仓库均不内置机器相关二进制。
- 自动部署成功后需重启 DaVinci Resolve；若目标机未显示“流程整合”菜单，仍可使用 XML 时间线导出。

# 1.0.0.279

- Resolve 插件升级至 0.2.2，安装器与设置页直接将软件 data 中的内置插件包复制到官方流程整合插件目录。
- SDK 缺失、Resolve 正在运行、版本或版本类型不再阻断文件部署；若检测到官方 SDK 原生模块则作为可选文件一并复制。
- 安装成功仅表示文件复制完成，插件能否由 Resolve 加载取决于用户目标机环境。

# 1.0.0.280

- 修复 Resolve 插件部署脚本采用 UTF-8 无 BOM 时被 Windows PowerShell 5.1 按 ANSI/GBK 误读，从而在 `Join-Path` 行报告伪语法错误的问题。
- 安装、卸载与脚本回归文件统一改为 UTF-8 BOM，并新增 BOM 自动断言及 Windows PowerShell 5.1 AST/完整执行回归。

# 1.0.0.281

- Resolve 插件安装脚本改为显式按 UTF-8 读取无 BOM 的中文 `manifest.xml`，避免 Windows PowerShell 5.1 的系统 ANSI 默认编码破坏 XML 标签。
- 自动化脚本测试现在使用与安装包一致的 UTF-8 无 BOM 中文清单，并在 Windows PowerShell 5.1 中完成真实解析和部署。
- Inno 安装失败弹窗会读取 UTF-8 错误日志并展示实际 PowerShell 异常，便于区分清单、权限、目录占用等不同失败原因。
- 软件“设置 → 插件”补安装也会通过提权后的脚本错误日志回传真实异常，并在读取后清理临时日志。

# 1.0.0.282

- Resolve 插件升级至 0.2.3，新建时间线会先启用自定义设置，再写入 FilmStoryboard 分辨率和帧率。
- 已保存的新时间线不会再因时间线唯一 ID 接口异常被清理；无法读取 ID 时仅对已有 FilmStoryboard 绑定启用唯一名称回退。
- Bridge 会返回真实失败阶段，插件状态页会同时显示最近请求的结果说明。
- 已在本机 Resolve 21.0.0.47 完成首次创建、自动打开、单素材写入、项目保存及第二次不变同步验证。

# 1.0.0.283

- 修复确认镜头页全局构建或单镜头构建后，下一步视频生成提示词框仍为空的问题。
- 视频生成草稿直接采用当前脚本已完成的构建提示词，拍摄脚本字段仅作为兼容回退；局部重构不会影响其他镜头。

# 1.0.0.284

- Resolve 插件升级至 0.2.4，使用 FFprobe 真实视频时长和源帧总数限制裁剪出点，修复直连 Resolve 时相邻素材之间出现 1 帧空白的问题。
- 受管时间线的素材起点、时长和源入点改为严格帧级匹配；Resolve 实际截短 1 帧时会自动重新同步，不再误报“无变化”。

# 1.0.0.285

- 修复连续镜头组完成构建后，生成视频页的可灵提示词被错误降级为默认参考句的问题。
- 多图构建提示词以“图片1至图片N作为/为……”开头时，现在会完整原样进入生成草稿。
