# 067 - 版本322安装包编译增量备份

## 备份类型

- 类型：发布构建模块增量备份。
- 基线：已验证版本 `1.0.0+321`。
- 当前：已验证版本 `1.0.0+322`。
- 范围：仅记录本轮版本递增与安装包构建直接相关改动，不覆盖其他业务模块。

## 模块目标

基于现有 Windows 发布链路，递增一个新版本并重新生成可安装的 `.exe` 安装包，同时校验主程序版本、安装包版本与 SHA-256 输出一致。

## 修改源码

- `pubspec.yaml`
  - Flutter 产品版本从 `1.0.0+321` 递增到 `1.0.0+322`。
- `lib/features/updater/domain/app_update_config.dart`
  - 应用内部版本与版本标签从 `1.0.0.321 / v1.0.0.321` 递增到 `1.0.0.322 / v1.0.0.322`。
- `installer/filmstoryboard.iss`
  - Inno Setup 安装包版本从 `1.0.0.321` 递增到 `1.0.0.322`。

## 核心代码前后对比

### Flutter 版本号

修改前：

```yaml
version: 1.0.0+321
```

修改后：

```yaml
version: 1.0.0+322
```

### 应用更新器版本常量

修改前：

```dart
static const currentVersion = '1.0.0.321';
static const currentVersionTag = 'v1.0.0.321';
```

修改后：

```dart
static const currentVersion = '1.0.0.322';
static const currentVersionTag = 'v1.0.0.322';
```

### 安装包版本

修改前：

```iss
#define MyAppVersion "1.0.0.321"
```

修改后：

```iss
#define MyAppVersion "1.0.0.322"
```

## 构建与校验记录

- `python scripts/prepare_release.py --version 1.0.0.322`：通过。
- `D:\flutter\bin\flutter.bat pub get`：通过。
- `scripts/ensure_ffmpeg_windows.ps1`：通过，复用已准备好的 FFmpeg。
- `D:\flutter\bin\flutter.bat build windows --release`：通过。
- `iscc installer/filmstoryboard.iss`：通过。
- `scripts/verify_release_artifact.ps1 -Version 1.0.0.322`：通过。

## 产物清单

- 主程序：`build/windows/x64/runner/Release/filmstoryboard.exe`
- 安装包：`dist/installer/filmstoryboard-Setup-1.0.0.322.exe`
- 校验文件：`dist/installer/filmstoryboard-Setup-1.0.0.322.exe.sha256`
- SHA-256：`42cd2f0347f0932122dbd9868b5fe0cbe4bb94d0958b5be4ff55e284265bb239`
- 大小：`422729141` 字节
- 签名状态：`NotSigned`

## 回滚定位

如需回滚本轮版本，仅需把上述三个文件恢复到 `321` 版本字段，并重新构建对应安装包；不应回滚其他未涉及业务逻辑的目录或并行工作内容。
